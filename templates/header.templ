package templates
 

templ header(header string) {
  <div class="w-full">
    <div class="w-full justify-center my-4">
      <h5 class="text-center font-bold text-5xl text-primary">{header}</h5>
    </div>
  </div>
}

